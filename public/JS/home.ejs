
// Get modal elements
const modal = document.getElementById("priceModal");
const updatePriceBtn = document.getElementById("update-price-btn");
const closeBtn = document.querySelector(".close-btn");
const savePriceBtn = document.getElementById("save-price-btn");

// Open the modal when "Update Price Per Unit" button is clicked
updatePriceBtn.onclick = function () {
      modal.style.display = "block";
};

// Close the modal when the close button is clicked
closeBtn.onclick = function () {
      modal.style.display = "none";
};

// Close the modal if the user clicks anywhere outside the modal
window.onclick = function (event) {
      if (event.target === modal) {
            modal.style.display = "none";
      }
};

// Handle the save button click
savePriceBtn.onclick = function () {
      const newPrice = document.getElementById("new-price").value;
      if (newPrice) {
            alert("Price per unit updated to $" + newPrice);
            // You can update the price in your application as needed
            modal.style.display = "none";
      } else {
            alert("Please enter a valid price");
      }
};
